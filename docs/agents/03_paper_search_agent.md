# Paper Search Agent
- **Purpose:** Discovers external literature regarding the claims using adversarial multi-query formulations.
- **Strategy:** Queries both confirmation phrases ("X improves") and counter-evidence phrases ("X limitations", "X fails").
- **Outputs:** List of candidate papers with metadata (title, authors, abstract, DOI, download URL).
